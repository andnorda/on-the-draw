{
  "name": "on-the-draw",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "fetch": "tsx scripts/fetch-data.ts",
    "fetch:force": "tsx scripts/fetch-data.ts --force",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.0.4"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
